import { HashZeroFormatPipe } from './hash-zero-format.pipe';

describe('HashZeroFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HashZeroFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
